/*
Base *bp, b_ob;
Derived *dp, d_ob;

bp = &d_ob; //указатель базового типа ссылается на объект производного класса
dp = dynamic_cast<Derieved *> (bp);//приведение указателя производного типа выполнено успешно

if(dp) std::cout << "Приведение выполенено успешно";
*/
